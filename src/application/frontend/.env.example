# For local development only
# The frontend automatically detects localhost and uses this URL
# In production, it automatically uses relative /api path (routed through CloudFront)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# Production builds should NOT set NEXT_PUBLIC_API_BASE_URL
# The app will automatically use /api path for all non-localhost domains